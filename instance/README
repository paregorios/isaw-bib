## You need to add a file called zotero.cfg with the required configuration variables
## in order for this flask app to work.
##
## Replace the xxx's with the correct variables and save everything below the hyphens
## as a file in this directory with the filename zotero.cfg.
## -----

# zotero.cfg
 
import os
 
# grab the folder of the top-level directory of this project
BASEDIR = os.path.abspath(os.path.dirname(__file__))
TOP_LEVEL_DIR = os.path.abspath(os.curdir)
 
# Update later by using a random number generator and moving
# the actual key outside of the source code under version control
DEBUG = True
        
LIBRARY_ID = 'XXX'
LIBRARY_TYPE = 'XXX'
API_KEY = 'XXX'